In this exercise, the implementation of a variational autoencoder with keras and tensorflow framework for automatic dierentiation of tensors.
Project the training data to latent space. 
Created a grid (of appropriate size) in the latent space and projected it to the data domain with the decoder.
